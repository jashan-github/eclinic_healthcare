// import axiosInstance from '@/lib/api'

export const getAllPatientReferrals = async () => {
  try {
    // TODO: Ricky[22/8/25] Add API Logic once its available
    // const { data } = await axiosInstance.get(
    //   '/v5/doctor/settings/treatment-services'
    // )

    return []
  } catch (error) {
    console.log(error)

    throw error
  }
}
