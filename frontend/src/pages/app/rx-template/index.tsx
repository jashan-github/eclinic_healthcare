import RxTemplateWrapper from '@/features/app/rx-template'
import { type FC, type ReactElement } from 'react'

const RxTemplatePage: FC = (): ReactElement => {
  return <RxTemplateWrapper />
}

export default RxTemplatePage
