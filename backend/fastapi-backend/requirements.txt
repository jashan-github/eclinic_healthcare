# eClinic Backend - Python Dependencies

# FastAPI and ASGI server
fastapi==0.109.0
uvicorn[standard]==0.27.0
python-multipart==0.0.6

# Pydantic for validation and settings
pydantic==2.5.3
pydantic-settings==2.1.0
email-validator==2.1.0

# Database
sqlalchemy==2.0.25
alembic==1.13.1
psycopg2-binary==2.9.9  # PostgreSQL adapter
asyncpg==0.29.0  # Async PostgreSQL adapter

# Redis
redis==5.0.1
hiredis==2.3.2  # Redis parser (performance)

# Authentication & Security
python-jose[cryptography]==3.3.0  # JWT
passlib[bcrypt]==1.7.4  # Password hashing
bcrypt==4.1.2
python-dotenv==1.0.0

# Logging
loguru==0.7.2

# HTTP client (for external API calls)
httpx==0.26.0
aiohttp==3.9.1

# Date/Time utilities
python-dateutil==2.8.2
pytz==2023.3

# Email
aiosmtplib==3.0.1  # Async SMTP
sendgrid==6.11.0  # SendGrid email service

# SMS/WhatsApp (Twilio)
twilio==8.11.1

# Firebase Cloud Messaging (push notifications)
firebase-admin==6.4.0

# Data validation & parsing
phonenumbers==8.13.27  # Phone number validation

# Testing
pytest==7.4.4
pytest-asyncio==0.23.3
pytest-cov==4.1.0
httpx==0.26.0  # For TestClient
faker==22.0.0  # Fake data generation

# Code quality
black==23.12.1  # Code formatter
flake8==7.0.0  # Linter
mypy==1.8.0  # Type checker
isort==5.13.2  # Import sorter

# Development tools
watchdog==3.0.0  # File system monitoring (for auto-reload)
ipython==8.19.0  # Interactive shell

# API documentation
fastapi[all]==0.109.0  # Includes swagger/redoc

# Agora Video SDK
# Note: Agora token generation is implemented manually (no official Python SDK)
# Using custom implementation with HMAC-SHA256 signing

# PDF Generation
reportlab==4.0.7  # PDF generation for invoices and receipts
